# Windows 前台视觉自动化工作台

## 完整开发任务与测试验收方案

| 项 | 内容 |
|---|---|
| 文档日期 | 2026-09-25 |
| 文档版本 | v1.0（实施基线稿） |
| 依据 | 《Windows 前台游戏脚本工作台——需求描述》v0.1；《Windows 前台视觉自动化工作台——GitHub 方案调研、安全边界与落地架构建议》v0.2 |
| 目标平台 | Windows 10/11 x64（具体系统构建在 M0 冻结） |
| 推荐技术主线 | Vue3 + TypeScript；Python 本地控制面与运行时；OpenCV；DXcam/WGC/DXGI 主采集、MSS 回退；声明式 YAML/JSON 状态机；Win32 SendInput 薄适配器 |
| 文档用途 | 任务拆分、研发排期、测试设计、里程碑准入、UAT 与发布签字 |
| 估算口径 | 人日为规划估算，已包含编码、单元测试、代码评审和必要文档，不等同于日历工期 |

---

## 1. 实施结论与不可变原则

本方案把产品实施目标冻结为：

> **可观察、可回放、可测试的 Windows 前台视觉自动化工作台。**

产品通过“屏幕采集 → 视觉感知 → 状态机决策 → 策略守卫 → 前台输入”形成闭环；所有真实输入都必须经过目标白名单、前台复核、模式、时限、配额、急停和审计控制。

### 1.1 八条架构不变量

1. UI、脚本和检测器均不得直接调用系统输入；只能产生 `InputIntent`。
2. 所有真实输入必须经过 `Policy Guard` 和 `Input Broker`。
3. 每批输入执行前重新核验 HWND、PID、目标实例、前台状态、会话和 TTL。
4. 失焦、急停、父进程退出、锁屏、会话切换或状态不确定时，先停止输入并释放全部按键。
5. 普通脚本使用声明式 YAML/JSON；M0-M3 禁止任意 Python、Shell、DLL 或系统命令执行。
6. 每次运行必须生成可追溯轨迹；同版本、同轨迹、同随机种子必须得到同一决策结果。
7. 真实输入优先用于 ArenaLab、本地、离线、自有或明确允许自动化的目标；受保护在线游戏配置默认只允许观察、标注、回放和 Shadow Mode。
8. 不实现进程隐藏、驱动隐藏、反作弊探测、内存读写、进程注入、封包截获/修改、硬件伪装、拟人化逃避检测、多账号集群或后台多开。

### 1.2 本次交付范围

**M0-M3 为首个可发布版本范围：**

- 项目、目标、标定、视觉资产、检测器、状态机、策略、测试与运行轨迹的完整领域模型；
- ArenaLab 本地测试程序；
- 前台窗口锁、屏幕采集、OpenCV 检测器、状态机和安全输入代理；
- 实时预览、ROI 框选、检测调试、运行时间轴、回放与差异分析；
- 项目版本、影响分析、签名、导入导出与整体回滚；
- Windows 自动化测试、性能、长稳、安全、安装升级和 UAT。

**M4-M5 为增强或受控适配范围：**节点编辑器、受限扩展、普通桌面目标模板、受保护在线目标的 Shadow 模板。它们不是首版发布阻塞项。

### 1.3 明确不作为验收目标

- 在线游戏无人值守自动刷怪成功率；
- “降低封号概率”“绕过检测”或任何反作弊对抗效果；
- 自动重连后继续长时间运行；
- 任意 Python 脚本市场、第三方脚本直接执行；
- 独占全屏、后台多开、跨账号调度；
- YOLO 训练平台或游戏专用识别模型。

---

## 2. 建议团队与工作方式

### 2.1 推荐角色

| 角色 | 建议投入 | 主要职责 |
|---|---:|---|
| 技术负责人/架构 | 1 | 架构不变量、ADR、跨模块契约、代码评审、发布准入 |
| Python/Windows 运行时 | 2 | 控制面、运行时、输入代理、采集、轨迹、打包 |
| 视觉算法 | 0.5~1 | OpenCV 检测器、数据集、指标和性能 |
| Vue3 前端 | 1 | 桌面工作台、编辑器、预览、时间轴、测试中心 |
| QA/测试开发 | 1 | 测试夹具、ArenaLab E2E、回放、性能、长稳、安全回归 |
| 产品/验收负责人 | 0.5 | 范围、优先级、UAT、风险豁免与最终签字 |

### 2.2 开发节奏

- 每个任务先满足 DoR，再进入开发；P0 安全任务不可被功能任务跳过。
- PR 必须通过静态检查、单测、契约测试；影响视觉/状态机/输入的变更必须附回放差异。
- 每周至少一次跨模块演示，以 ArenaLab 场景为准，不以真实在线游戏作为开发调试环境。
- 每个里程碑都生成候选版、测试报告、未关闭缺陷和风险豁免清单。

### 2.3 Definition of Ready（DoR）

任务进入开发前必须具备：目标、范围外内容、依赖、输入样例、接口或 UI 草图、可验证验收标准、测试责任人和数据准备说明。

### 2.4 Definition of Done（DoD）

任务完成必须同时满足：代码合并、类型检查和 Lint 通过；单元/组件测试；错误与取消路径；结构化日志；必要文档；无未声明能力；相关回放/黄金数据更新；QA 可独立复现；无 P0/P1 遗留缺陷。

---

## 3. 里程碑与阶段准入

| 里程碑 | 目标 | 主要交付 | 退出条件 |
|---|---|---|---|
| M0 边界与测试场 | 证明架构可落地且不依赖真实游戏 | ADR、Schema、领域模型、ArenaLab、轨迹格式、工程底座 | 示例项目可校验；ArenaLab 可确定重放；所有动作先形成 InputIntent；在线目标默认无真实输入 |
| M1 安全闭环 | 建立桌面壳、采集、目标锁和安全输入 | pywebview 壳、目标选择、采集、Dry/Shadow、Input Broker、急停、失焦停止、基础时间轴 | 错目标/失焦/急停/进程退出真实输入均为 0 或立即停止；无卡键；P0 安全用例 100% 通过 |
| M2 可测试自动化 | 完成视觉、状态机、回放和测试自动化 | 模板/颜色/变化/OCR、DSL 编译、静态检查、固定感知与原始帧回放、测试中心基础 | 同轨迹确定性；视觉指标达标；变更可生成差异；ArenaLab E2E 通过 |
| M3 可发布工作台 | 完成资产、版本、发布、回滚、隐私和安装 | 资产库、编辑器、发布包、影响分析、签名、回滚、安装器、诊断包、完整测试报告 | 候选版通过全量回归、性能、长稳、安全与 UAT；无 P0/P1 缺陷；可完整回滚 |
| M4 易用性增强 | 节点编辑和受限扩展 | Rete.js 节点视图、撤销重做、扩展 Runner 原型 | 节点图与 DSL 单一语义；扩展不能绕过策略和输入代理 |
| M5 受控适配 | 支持允许自动化的目标和 Shadow 模板 | 普通桌面模板、受保护在线目标 Shadow 模板、适配准入机制 | 真实输入只在授权目标启用；受保护目标硬锁测试 100% 通过 |

### 3.1 估算汇总

- 完整基线共 **172 项任务**，规划估算 **967 人日**；其中 M4/M5 属于可选增强。
- P0 为首版安全与核心能力；P1 为可用性和产品化；P2 为后续增强。
- 建议实际排期时保留 15%~25% 的 Windows 兼容、性能调优和缺陷修复缓冲。

| 里程碑 | 规划人日 |
|---|---:|
| M0 | 96 |
| M1 | 273 |
| M2 | 328 |
| M3 | 189 |
| M4 | 55 |
| M5 | 26 |

| 优先级 | 规划人日 |
|---|---:|
| P0 | 737 |
| P1 | 175 |
| P2 | 55 |

---

## 4. 完整开发任务清单

> 依赖列使用任务 ID；多项依赖以逗号分隔。人日仅用于初排，开发前需结合团队熟悉度再校准。

### E00 范围、架构与项目治理

**目标：** 冻结产品边界、关键架构不变量、交付与评审机制，避免后续把平台演化成不可测试的脚本集合。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| GOV-001 | 冻结产品范围与禁止能力清单 | P0 | M0 | 产品/TL | 2 | - | 形成经评审的范围基线；明确不做注入、读写内存、封包、反作弊绕过、后台多开、任意 Python 脚本执行。 |
| GOV-002 | 建立架构决策记录 ADR | P0 | M0 | TL | 3 | GOV-001 | 至少覆盖进程划分、IPC、存储、桌面壳、脚本 DSL、输入代理、回放与插件边界。 |
| GOV-003 | 建立威胁模型与滥用场景清单 | P0 | M0 | TL/安全 | 3 | GOV-001 | 覆盖错误窗口输入、脚本越权、恶意导入包、路径穿越、本地 API 劫持、按键卡死、日志隐私泄露。 |
| GOV-004 | 确定兼容矩阵与基准机 | P0 | M0 | TL/QA | 2 | GOV-002 | 冻结 Windows x64、分辨率、DPI、多屏、权限级别及性能基准机定义。 |
| GOV-005 | 确定版本、分支与发布策略 | P0 | M0 | TL/发布 | 2 | GOV-002 | 定义 SemVer、Schema 版本、兼容范围、分支保护、候选版和回滚策略。 |
| GOV-006 | 建立 DoR/DoD 与评审模板 | P0 | M0 | TL/QA | 2 | GOV-001 | 所有任务具备输入、依赖、验收标准；完成标准包含测试、文档、日志、错误处理和代码评审。 |

### E01 工程底座与持续集成

**目标：** 建立可重复构建、可在 Windows 上持续测试、可追踪依赖和产物的单仓工程。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| ENG-001 | 初始化 Monorepo 与目录结构 | P0 | M0 | TL/全栈 | 3 | GOV-002 | 建立 apps/services/packages/schemas/tests/docs/tools 目录及模块所有权规则。 |
| ENG-002 | 建立 Python 依赖锁定与开发环境 | P0 | M0 | 后端 | 3 | ENG-001 | 一条命令创建环境、安装锁定依赖并运行单测；禁止未锁定生产依赖。 |
| ENG-003 | 建立 Vue3/TypeScript 工程与依赖锁定 | P0 | M0 | 前端 | 3 | ENG-001 | 完成 Vite、状态管理、路由、测试框架和统一脚本。 |
| ENG-004 | 统一格式化、Lint、类型检查与提交钩子 | P0 | M0 | 全栈 | 3 | ENG-002,ENG-003 | PR 中自动执行 Python/TypeScript 静态检查；主分支不允许带错误合并。 |
| ENG-005 | 实现结构化日志与关联 ID | P0 | M0 | 后端 | 4 | ENG-002 | 会话、帧、检测、状态迁移、意图和策略事件可用 correlation_id 串联。 |
| ENG-006 | 建立 Windows CI 流水线 | P0 | M0 | QA/发布 | 5 | ENG-002,ENG-003,ENG-004 | PR 执行静态检查和单测；主分支执行 Windows 集成测试并归档报告。 |
| ENG-007 | 建立制品与测试报告归档 | P1 | M0 | 发布/QA | 3 | ENG-006 | 安装包、符号、SBOM、覆盖率、性能结果、回放差异可按构建号追踪。 |
| ENG-008 | 提供开发者启动与诊断脚本 | P1 | M0 | 全栈 | 3 | ENG-001 | 新环境可在 30 分钟内启动 UI、控制面、运行时和 ArenaLab。 |

### E02 领域模型、Schema 与脚本编译器

**目标：** 把项目、目标、视觉资产、检测器、状态机、策略、轨迹和测试用例变成可版本化、可静态检查的契约。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| DOM-001 | 定义核心领域对象与类型 | P0 | M0 | TL/后端 | 5 | GOV-002 | 完成 TargetProfile、CalibrationProfile、VisualAsset、Detector、PerceptionSnapshot、StateMachine、InputIntent、PolicyProfile、RunTrace、TestCase。 |
| DOM-002 | 编写项目与各对象 JSON Schema | P0 | M0 | 后端 | 6 | DOM-001 | Schema 能校验必填项、枚举、引用和版本；提供正反样例。 |
| DOM-003 | 实现 Schema 校验与可定位错误 | P0 | M0 | 后端 | 4 | DOM-002 | 错误返回文件、JSON Pointer、规则和修复提示；UI 可直接定位字段。 |
| DOM-004 | 实现 Schema 迁移框架 | P1 | M2 | 后端 | 5 | DOM-002,GOV-005 | 支持向前迁移、预检、备份和失败回滚；迁移结果可重复。 |
| DOM-005 | 定义脚本能力清单与授权模型 | P0 | M0 | TL/安全 | 4 | DOM-001,GOV-003 | 能力默认拒绝；脚本只能声明注册能力，不能绕过 Policy Guard。 |
| DOM-006 | 实现 DSL 解析器与中间表示 AST | P0 | M1 | 后端 | 6 | DOM-002 | YAML/JSON 解析为同一 AST；保留源位置；解析错误不启动运行时。 |
| DOM-007 | 实现 DSL 静态分析器 | P0 | M2 | 后端/QA | 8 | DOM-006 | 检查不可达状态、无退出非终态、无限重试、未定义字段、缺失资产、越权动作及在线目标无人值守配置。 |
| DOM-008 | 实现项目包引用解析与内容哈希 | P0 | M2 | 后端 | 5 | DOM-002 | 模板、标定、检测器、状态机和策略引用可解析且哈希固定。 |
| DOM-009 | 提供 ArenaLab 示例项目与规范示例 | P0 | M0 | 后端/文档 | 3 | DOM-002 | 示例可通过全部 Schema 校验并作为测试夹具。 |

### E03 ArenaLab 本地可控测试场

**目标：** 建立不依赖真实在线游戏、可确定复现视觉和输入问题的本地模拟器。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| LAB-001 | 实现 ArenaLab 基础窗口与确定性渲染 | P0 | M0 | 前端/测试工具 | 6 | ENG-001,GOV-004 | 固定种子下同一场景生成同一帧序列；窗口可被目标选择器识别。 |
| LAB-002 | 实现血条、资源条、冷却、目标与掉落控件 | P0 | M0 | 测试工具 | 6 | LAB-001 | 可用脚本或控制面板改变状态，并提供期望值 Oracle。 |
| LAB-003 | 实现加载画面、弹窗、断线样式与遮挡 | P0 | M0 | 测试工具 | 5 | LAB-001 | 可按场景触发并准确记录开始/结束时间。 |
| LAB-004 | 实现可版本化场景脚本与随机种子 | P0 | M0 | 测试工具 | 5 | LAB-002,LAB-003 | 支持 happy path、异常路径和边界路径；每次运行可重放。 |
| LAB-005 | 支持分辨率、DPI、UI 缩放和窗口尺寸切换 | P1 | M1 | 测试工具 | 5 | LAB-001 | 至少覆盖测试矩阵中的三档分辨率与三档 DPI。 |
| LAB-006 | 支持掉帧、输入延迟、失焦、遮挡和卡顿注入 | P0 | M1 | 测试工具/QA | 6 | LAB-004 | 故障可由测试用例精确触发，且不会修改被测系统代码。 |
| LAB-007 | 实现测试 Oracle 与事件导出 | P0 | M1 | 测试工具/QA | 5 | LAB-004 | 导出真实状态、接受到的输入及时间戳，供 E2E 自动断言。 |
| LAB-008 | ArenaLab 独立构建与 CI 集成 | P1 | M1 | 发布/QA | 3 | LAB-007,ENG-006 | CI 可启动、驱动并退出 ArenaLab，不残留进程。 |

### E04 目标窗口、前台锁与标定

**目标：** 确保系统只面向已确认目标工作，并在不同 DPI、分辨率和多屏环境中保持坐标一致。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| TGT-001 | 实现窗口与进程枚举服务 | P0 | M1 | 后端 | 4 | ENG-002 | 展示 HWND、PID、进程名、标题、可见性、最小化状态和显示器信息。 |
| TGT-002 | 实现 TargetProfile 匹配与白名单 | P0 | M1 | 后端/安全 | 6 | DOM-001,TGT-001 | 支持可执行文件、标题正则、窗口类和可选签名/哈希规则；模糊匹配不得自动放行。 |
| TGT-003 | 实现前台窗口实时监视 | P0 | M1 | 后端 | 5 | TGT-001 | 状态变化以事件发送；轮询或钩子失效时进入安全停止。 |
| TGT-004 | 实现权限级别/UIPI 诊断 | P0 | M1 | 后端 | 4 | TGT-001 | 目标权限高于工作台时显示明确诊断并拒绝假成功。 |
| TGT-005 | 实现 DPI 感知与坐标转换 | P0 | M1 | 后端/CV | 7 | GOV-004 | 窗口坐标、客户区坐标、屏幕坐标和归一化 ROI 双向转换通过矩阵测试。 |
| TGT-006 | 实现多显示器与显示配置变更处理 | P1 | M1 | 后端 | 5 | TGT-005 | 目标跨屏、主屏切换、显示器断开后停止并要求重新标定。 |
| TGT-007 | 实现标定向导与锚点配置 | P0 | M2 | 前端/CV | 7 | TGT-005,UI-004 | 可从当前帧选择锚点，生成可预览的 CalibrationProfile。 |
| TGT-008 | 实现标定兼容性检查 | P0 | M2 | 后端 | 4 | TGT-007 | 分辨率、DPI、窗口尺寸或 UI 缩放不兼容时禁止真实输入。 |
| TGT-009 | 实现单会话目标锁 | P0 | M1 | 后端 | 4 | TGT-002 | 会话启动后绑定 PID/HWND/实例；目标重启或替换必须重新人工确认。 |

### E05 屏幕采集与帧管线

**目标：** 提供可替换、可诊断、时间戳稳定的 Windows 前台采集能力，并在降级时保持安全。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| CAP-001 | 定义 Capture API 与帧数据结构 | P0 | M1 | 后端/CV | 4 | DOM-001 | 统一像素格式、尺寸、时间戳、目标信息、ROI 和帧序号。 |
| CAP-002 | 实现 DXcam/WGC/DXGI 主采集适配器 | P0 | M1 | 后端/CV | 8 | CAP-001,TGT-009 | 可采集目标客户区；异常返回明确状态而不是旧帧。 |
| CAP-003 | 实现 python-mss 兼容回退适配器 | P0 | M1 | 后端/CV | 5 | CAP-001 | 接口与主适配器一致；可手动或自动降级。 |
| CAP-004 | 实现帧环形缓冲与单调时钟 | P0 | M1 | 后端 | 5 | CAP-001 | 消费者不会读到半写帧；时间戳单调；过期帧可识别。 |
| CAP-005 | 实现 ROI 裁剪、缩放与色彩转换 | P0 | M1 | CV | 5 | CAP-004,TGT-005 | ROI 边界安全；转换结果与基准图像一致。 |
| CAP-006 | 实现背压、丢帧和消费者隔离 | P0 | M1 | 后端 | 5 | CAP-004 | 慢检测器不拖死采集；丢帧有计数和告警。 |
| CAP-007 | 实现采集故障检测与适配器切换 | P1 | M2 | 后端 | 5 | CAP-002,CAP-003 | 黑帧、尺寸异常、设备丢失时停止输入并可安全切换。 |
| CAP-008 | 实现采集诊断页与基准工具 | P1 | M1 | 后端/前端 | 5 | CAP-002,CAP-003 | 显示 FPS、延迟、丢帧、适配器、色彩格式和窗口几何。 |

### E06 视觉检测与调试

**目标：** 建立以 ROI、模板、颜色和变化检测为核心的可测感知层；OCR 仅按事件和小区域调用。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| VIS-001 | 定义 Detector 接口、注册表与输出契约 | P0 | M2 | CV/后端 | 5 | DOM-001,CAP-001 | 输出值、置信度、框、耗时、版本和错误状态统一。 |
| VIS-002 | 实现基础模板匹配检测器 | P0 | M2 | CV | 7 | VIS-001,CAP-005 | 支持阈值、ROI、最佳匹配和多个候选；有正负样本测试。 |
| VIS-003 | 实现模板掩码、尺度与多模板策略 | P1 | M2 | CV | 7 | VIS-002 | 支持 alpha/显式掩码、有限尺度集和模板优先级。 |
| VIS-004 | 实现颜色条比例检测器 | P0 | M2 | CV | 6 | VIS-001 | 可输出 0~1 比例、置信度和异常值；对亮度变化有基线鲁棒性。 |
| VIS-005 | 实现颜色区域/像素统计检测器 | P0 | M2 | CV | 5 | VIS-001 | 支持 HSV/RGB 范围、面积比例和连通域过滤。 |
| VIS-006 | 实现画面变化、稳定与卡死检测器 | P0 | M2 | CV | 6 | VIS-001 | 支持差分、稳定帧、阈值窗口和 ROI；可区分加载动画与静止。 |
| VIS-007 | 实现事件触发式 ROI OCR | P1 | M2 | CV | 8 | VIS-001 | 仅对指定 ROI 和触发条件运行；有超时、语言和空结果处理。 |
| VIS-008 | 实现检测器调度、频率与计算预算 | P0 | M2 | 后端/CV | 6 | VIS-001 | 每个检测器可配置频率/优先级；超预算时降级且不阻塞安全检测。 |
| VIS-009 | 实现稳定帧、迟滞和置信度聚合 | P0 | M2 | CV | 5 | VIS-001 | 避免单帧抖动；聚合逻辑可确定回放。 |
| VIS-010 | 实现检测框、热力图和 ROI 调试叠加 | P0 | M2 | CV/前端 | 6 | VIS-002,UI-004 | 实时预览可开关叠加，显示置信度、耗时和资产版本。 |
| VIS-011 | 实现视觉黄金数据集与指标计算器 | P0 | M2 | CV/QA | 7 | VIS-002,VIS-004,VIS-006 | 输出 precision、recall、定位误差、比例 MAE、耗时及版本差异。 |
| VIS-012 | 实现检测器离线批量运行与差异报告 | P0 | M2 | CV/QA | 6 | VIS-011 | 资产或阈值变更后自动比较基线并标出退化样本。 |
| VIS-013 | 预留模型检测器接口但不引入训练平台 | P2 | M4 | CV | 3 | VIS-001 | 只定义受限输入输出、模型哈希和资源预算；M0-M3 不依赖模型。 |

### E07 状态机、条件规则与确定性运行时

**目标：** 把感知快照编译为可测试、可暂停、可取消且不会直接操作系统的状态机执行。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| FSM-001 | 定义安全条件表达式语法 | P0 | M2 | 后端 | 6 | DOM-006 | 只允许白名单运算和感知字段；禁止任意代码、反射和系统调用。 |
| FSM-002 | 实现状态机编译器 | P0 | M2 | 后端 | 8 | DOM-006,FSM-001 | 把 AST 编译为运行图；错误映射回源文件。 |
| FSM-003 | 实现状态、守卫、迁移与优先级语义 | P0 | M2 | 后端 | 7 | FSM-002 | 同一快照下迁移选择确定；冲突有静态或运行时诊断。 |
| FSM-004 | 实现 entry/exit/action 与 InputIntent 生成 | P0 | M2 | 后端 | 6 | FSM-002,INP-001 | 运行时只产生意图，不直接调用输入 API。 |
| FSM-005 | 实现可注入时钟、定时器与超时 | P0 | M2 | 后端/QA | 6 | FSM-002 | 单测可无等待推进时间；所有定时行为可回放。 |
| FSM-006 | 实现暂停、继续、取消和安全停止 | P0 | M2 | 后端 | 6 | FSM-004,FSM-005 | 取消后不产生新意图；恢复前重新验证目标和标定。 |
| FSM-007 | 实现 wait_until 与 assert_after | P0 | M2 | 后端 | 6 | FSM-005 | 动作后可等待可观察条件；失败进入明确错误出口。 |
| FSM-008 | 实现有界重试、退避和异常路由 | P0 | M2 | 后端 | 6 | FSM-005 | 禁止无预算无限重试；每次重试有原因和计数。 |
| FSM-009 | 实现确定性随机源与决策哈希 | P1 | M2 | 后端 | 4 | FSM-003 | 同版本、同轨迹、同种子输出相同状态和意图哈希。 |
| FSM-010 | 实现状态/迁移覆盖率采集 | P1 | M2 | 后端/QA | 5 | FSM-003 | 测试报告能展示状态、迁移、守卫和异常路径覆盖。 |
| FSM-011 | 实现单步调试与断点（仅 Dry Run） | P1 | M3 | 后端/前端 | 6 | FSM-006,UI-013 | 可逐帧/逐迁移调试；单步模式禁止真实输入。 |

### E08 策略守卫与安全输入代理

**目标：** 把所有动作置于目标、焦点、模式、时限和预算控制之下，保证错误条件下真实输入为零。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| POL-001 | 实现 PolicyProfile 解析与策略求值器 | P0 | M1 | 后端/安全 | 6 | DOM-002,DOM-005 | 策略默认拒绝；每次拒绝返回机器可读原因。 |
| POL-002 | 实现 Observe/Shadow/DryRun/RealInput 模式机 | P0 | M1 | 后端 | 5 | POL-001 | 模式切换有人工闸门和审计；Shadow/DryRun 不调用 OS 输入。 |
| POL-003 | 实现受保护在线目标的真实输入硬锁 | P0 | M1 | 安全/TL | 4 | POL-001,TGT-002 | 被标记为 protected_online 的目标默认只能观察、标注、回放和 Shadow。 |
| POL-004 | 实现运行时长、时间窗和会话总量限制 | P0 | M1 | 后端 | 5 | POL-001 | 脚本不能提高策略上限；到限立即进入不可自动恢复停止态。 |
| POL-005 | 实现动作数、连续按键、鼠标距离和重试预算 | P0 | M1 | 后端 | 6 | POL-001 | 每项预算独立计数；超限拒绝并触发停止或人工处理。 |
| INP-001 | 定义 InputIntent、批次与过期语义 | P0 | M1 | 后端 | 5 | DOM-001 | 支持键盘、鼠标、滚轮和等待；每批含 session、target、TTL、cause。 |
| INP-002 | 实现 FakeInputSink | P0 | M1 | 后端/QA | 4 | INP-001 | 完整记录将执行动作；可注入失败和延迟；绝不调用系统输入。 |
| INP-003 | 实现 Win32 SendInput 适配器 | P0 | M1 | Windows 后端 | 8 | INP-001,TGT-004 | 支持扫描码、鼠标、批次结果和错误码；不提供注入、驱动或后台输入。 |
| INP-004 | 实现按键状态账本与幂等释放 | P0 | M1 | Windows 后端 | 6 | INP-003 | 任何停止路径都能释放所有已按下键；重复释放无副作用。 |
| INP-005 | 实现每批动作前目标复核与 TTL 校验 | P0 | M1 | 后端/安全 | 6 | INP-003,TGT-003,TGT-009 | 前台 HWND/PID/实例不一致或意图过期时真实输入为 0。 |
| INP-006 | 实现全局急停热键 | P0 | M1 | Windows 后端 | 6 | INP-004 | 独立于 UI 线程；触发后先停止输入和释放键，再清理会话。 |
| INP-007 | 实现父进程/代理看门狗与崩溃清理 | P0 | M1 | Windows 后端 | 6 | INP-004 | 父进程退出、心跳中断或 Broker 异常后自动释放键并拒绝新意图。 |
| INP-008 | 实现失焦、锁屏、会话切换和远程桌面策略 | P0 | M1 | Windows 后端 | 6 | INP-005 | 任何不确定前台状态都停止；不自动抢焦点。 |
| INP-009 | 实现输入调度、取消和顺序保证 | P0 | M1 | 后端 | 6 | INP-003,INP-005 | 同一会话顺序可预测；取消屏障后旧批次不得执行。 |
| INP-010 | 实现安全事件审计与恢复指引 | P1 | M1 | 后端/前端 | 4 | INP-005,INP-006 | 记录拒绝、急停、失焦、预算耗尽和按键释放结果。 |

### E09 运行轨迹、回放与差异分析

**目标：** 让每次识别、决策、策略拒绝和输入结果都可追溯，并支持不依赖真实目标的确定性回归。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| TRC-001 | 定义 RunTrace 事件与版本化格式 | P0 | M0 | 后端/QA | 6 | DOM-001 | 覆盖帧引用、感知、状态、意图、策略、执行、异常和版本哈希。 |
| TRC-002 | 实现追加写、批量刷新与崩溃容错 | P0 | M1 | 后端 | 6 | TRC-001,ENG-005 | 崩溃前已确认事件可读取；尾部损坏可检测和截断。 |
| TRC-003 | 实现帧内容寻址存储与索引 | P0 | M2 | 后端 | 6 | TRC-001 | 重复帧去重；引用不存在时明确报错；原始帧可按策略关闭。 |
| TRC-004 | 实现跨模块时间同步与序列号 | P0 | M1 | 后端 | 4 | TRC-001,CAP-004 | 同一事件链时序单调，可处理系统时钟跳变。 |
| TRC-005 | 实现固定感知回放 | P0 | M2 | 后端/QA | 6 | TRC-002,FSM-002 | 跳过视觉重放 PerceptionSnapshot，输出状态/意图差异。 |
| TRC-006 | 实现原始帧回放 | P0 | M2 | CV/QA | 7 | TRC-003,VIS-001 | 重新运行视觉管线，比较感知、状态和意图。 |
| TRC-007 | 实现差异引擎与可读报告 | P0 | M2 | 后端/前端 | 7 | TRC-005,TRC-006 | 按首个分歧点、受影响对象和严重度展示差异。 |
| TRC-008 | 实现时间轴查询 API | P0 | M2 | 后端 | 5 | TRC-002 | 支持按时间、状态、检测器、策略拒绝和异常筛选。 |
| TRC-009 | 实现轨迹导出、脱敏与导入校验 | P0 | M3 | 后端/安全 | 6 | TRC-002,SEC-005 | 默认去除账号名、路径、窗口标题敏感段和非必要原图。 |
| TRC-010 | 实现保留策略与安全清理 | P1 | M3 | 后端 | 4 | TRC-003,SEC-006 | 按项目/会话/年龄清理；被发布版本引用的基线不得误删。 |

### E10 控制面、本地 API 与存储

**目标：** 为桌面 UI 提供单机、受保护、可恢复的项目与运行会话服务。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| CTL-001 | 实现仅回环绑定的本地 API 与随机令牌 | P0 | M1 | 后端/安全 | 6 | ENG-002,GOV-003 | 只监听 loopback；每次启动生成令牌；无令牌请求被拒绝。 |
| CTL-002 | 实现 Origin/Host 校验与 API 版本 | P0 | M1 | 后端/安全 | 5 | CTL-001 | 拒绝未知 Origin、Host 和不兼容协议版本。 |
| CTL-003 | 实现项目、目标、策略与测试 CRUD | P0 | M1 | 后端 | 7 | DOM-002,CTL-001 | 写入前校验；更新采用乐观锁或版本号，避免覆盖。 |
| CTL-004 | 实现视觉资产与文件存储服务 | P0 | M2 | 后端 | 7 | DOM-008,CTL-003 | 限制路径、类型和大小；内容哈希去重；不可越出项目根目录。 |
| CTL-005 | 实现运行会话生命周期管理 | P0 | M1 | 后端 | 7 | POL-002,TGT-009 | 启动、暂停、恢复、停止、失败状态明确；停止幂等。 |
| CTL-006 | 实现单实例/单目标并发约束 | P0 | M1 | 后端 | 4 | CTL-005 | 同一 Input Broker 仅允许一个 RealInput 会话。 |
| CTL-007 | 实现 WebSocket 事件总线 | P0 | M1 | 后端 | 6 | CTL-001,ENG-005 | 支持重连、序列号、丢消息检测；高帧率原图不经 JSON Base64。 |
| CTL-008 | 实现配置、用户偏好与安全默认值 | P1 | M1 | 后端 | 4 | CTL-003 | 默认 Shadow、短保留期、禁用定时自动启动。 |
| CTL-009 | 实现崩溃恢复与脏会话修复 | P0 | M2 | 后端 | 6 | CTL-005,INP-007,TRC-002 | 重启后发现未正常结束会话，标记中断并执行安全清理。 |
| CTL-010 | 实现健康检查与诊断快照 | P1 | M2 | 后端 | 5 | CTL-005 | 汇总进程、线程、队列、采集、视觉、Broker、存储和版本状态。 |

### E11 桌面工作台 UI

**目标：** 提供项目、资产、检测器、状态机、运行、回放和测试的完整可观察工作流。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| UI-001 | 实现 pywebview 桌面壳与启动流程 | P0 | M1 | 前端/后端 | 6 | ENG-003,CTL-001 | 桌面壳启动/关闭能正确管理后端子进程；异常退出触发 Broker 清理。 |
| UI-002 | 实现项目导航与对象树 | P0 | M1 | 前端 | 5 | CTL-003 | 目标、标定、资产、检测器、状态机、测试、运行记录可导航。 |
| UI-003 | 实现目标选择与会话确认页 | P0 | M1 | 前端 | 6 | TGT-002,CTL-005 | 清楚展示 PID/HWND/风险模式；RealInput 需要显式人工确认。 |
| UI-004 | 实现实时画面预览 | P0 | M1 | 前端/后端 | 7 | CAP-004,CTL-007 | 显示帧率、延迟、目标几何和采集状态；预览卡顿不影响急停。 |
| UI-005 | 实现 ROI 框选与归一化坐标编辑 | P0 | M2 | 前端/CV | 7 | UI-004,TGT-005 | 支持拖拽、缩放、数值编辑和边界校验。 |
| UI-006 | 实现视觉资产库 | P0 | M3 | 前端 | 7 | CTL-004 | 支持截图入库、命名、标签、版本、掩码、引用关系和删除保护。 |
| UI-007 | 实现检测器配置与实时预览 | P0 | M2 | 前端/CV | 8 | VIS-001,UI-005 | 编辑阈值立即显示离线/实时结果，但不会直接写入发布版本。 |
| UI-008 | 实现标定向导 | P0 | M2 | 前端/CV | 7 | TGT-007,UI-005 | 完成锚点、校验点、预览和兼容性提示。 |
| UI-009 | 实现状态机表单编辑器 | P0 | M3 | 前端/后端 | 9 | FSM-002,CTL-003 | 可编辑状态、迁移、守卫、动作、超时和错误出口。 |
| UI-010 | 实现 YAML/JSON 文本编辑与双向同步 | P1 | M3 | 前端 | 7 | DOM-003,UI-009 | 表单与文本共享同一 AST；冲突时不静默覆盖。 |
| UI-011 | 实现只读状态图与覆盖率着色 | P1 | M3 | 前端 | 6 | FSM-010,UI-009 | 图只展示 DSL 语义；支持定位不可达和未覆盖迁移。 |
| UI-012 | 实现启动/暂停/恢复/停止控制条 | P0 | M1 | 前端 | 5 | CTL-005,POL-002 | 模式明显区分；停止操作始终可用并显示清理进度。 |
| UI-013 | 实现实时检查器 | P0 | M2 | 前端 | 6 | CTL-007,VIS-010,FSM-003 | 展示当前检测结果、状态时长、候选迁移、预算和拒绝原因。 |
| UI-014 | 实现运行时间轴 | P0 | M2 | 前端 | 8 | TRC-008 | 点击事件可还原当时帧、检测框、状态、意图与策略结果。 |
| UI-015 | 实现测试中心与回放差异页 | P0 | M3 | 前端/QA | 8 | TRC-007,TST-005 | 批量运行测试、查看失败样本、批准/拒绝新基线。 |
| UI-016 | 实现策略、隐私与保留期设置 | P1 | M3 | 前端/安全 | 5 | POL-001,SEC-005,SEC-006 | 高风险选项需说明与二次确认；受保护目标硬锁不可由 UI 解锁。 |
| UI-017 | 实现错误态、空态和可访问性 | P1 | M3 | 前端/QA | 5 | UI-002 | 核心流程支持键盘操作；错误不只用颜色表达。 |

### E12 版本包、发布、影响分析与回滚

**目标：** 确保每次发布冻结脚本、资产、阈值、标定、策略和测试结果，并能够整体回滚。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| VER-001 | 定义项目包 Manifest 与发布单元 | P0 | M2 | 后端/TL | 5 | DOM-008,GOV-005 | Manifest 冻结 Schema、运行时范围、资产、模型、策略和测试摘要。 |
| VER-002 | 实现内容哈希与不可变发布目录 | P0 | M2 | 后端 | 5 | VER-001 | 发布后内容不可原地修改；任何变化产生新版本。 |
| VER-003 | 实现草稿快照与历史记录 | P0 | M3 | 后端 | 6 | CTL-003,VER-002 | 可创建命名快照并比较对象级差异。 |
| VER-004 | 实现依赖图与变更影响分析 | P0 | M3 | 后端/QA | 8 | DOM-008,VIS-011,FSM-010 | 模板→检测器→感知字段→迁移→测试→项目可追踪。 |
| VER-005 | 实现发布流水线与质量闸门 | P0 | M3 | 后端/QA | 7 | VER-001,TST-011 | 静态检查、感知、状态机、回放、E2E 未通过时不能发布。 |
| VER-006 | 实现发布包签名与校验 | P1 | M3 | 安全/发布 | 6 | VER-001,SEC-004 | 导入前验证签名/哈希；未知签名默认不启用真实输入。 |
| VER-007 | 实现导入/导出与安全预检 | P0 | M3 | 后端/安全 | 6 | VER-001,SEC-002 | 展示能力、目标、文件和差异；拒绝恶意路径和超限包。 |
| VER-008 | 实现整体回滚 | P0 | M3 | 后端 | 6 | VER-002,VER-003 | 状态机、资产、阈值、标定和策略一起回滚；运行记录仍引用原版本。 |
| VER-009 | 实现升级备份与失败恢复 | P0 | M3 | 后端/发布 | 6 | DOM-004,VER-008 | 升级前自动备份；迁移失败恢复到原可启动版本。 |
| VER-010 | 实现运行时兼容矩阵检查 | P1 | M3 | 后端 | 4 | VER-001 | 不兼容项目包禁止运行并给出迁移建议。 |

### E13 自动化测试平台与质量工程

**目标：** 把安全、感知、状态机、回放、Windows E2E、性能和故障恢复纳入持续回归。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| TST-001 | 实现 FakeClock/FakeWindow/FakeCapture/FakeInput 测试套件 | P0 | M1 | QA/后端 | 7 | CAP-001,INP-002 | 单测无需真实桌面即可模拟时间、焦点、帧和输入结果。 |
| TST-002 | 建立核心模块单元测试基线 | P0 | M1 | 全体/QA | 8 | TST-001 | Policy/Input/FSM 分支覆盖达到质量闸门，关键安全分支 100% 用例覆盖。 |
| TST-003 | 建立属性测试与 DSL/Schema 模糊测试 | P1 | M2 | QA/后端 | 7 | DOM-002,DOM-006 | 随机输入不会导致崩溃、越权或无限循环。 |
| TST-004 | 建立模块契约与 IPC 兼容测试 | P0 | M2 | QA/后端 | 6 | CTL-002,DOM-002 | 前后端、运行时、Broker 的消息版本不兼容时明确失败。 |
| TST-005 | 建立视觉黄金数据集流水线 | P0 | M2 | CV/QA | 7 | VIS-011 | 数据、标签、划分、指标和基线均版本化。 |
| TST-006 | 建立回放回归套件 | P0 | M2 | QA/后端 | 7 | TRC-005,TRC-006 | 每次变更自动运行固定感知和原始帧回放。 |
| TST-007 | 建立 Windows ArenaLab E2E | P0 | M2 | QA | 10 | LAB-008,UI-012,INP-003 | 自动覆盖启动、标定、运行、失焦、急停、停止和清理。 |
| TST-008 | 建立性能基准与趋势门禁 | P1 | M2 | QA/性能 | 7 | CAP-008,VIS-011 | 记录采集、视觉、状态机、事件、UI 延迟和资源占用趋势。 |
| TST-009 | 建立长稳、故障注入与恢复测试 | P0 | M3 | QA | 8 | LAB-006,CTL-009,INP-007 | 覆盖进程崩溃、设备丢失、磁盘满、日志损坏、网络栈不可用等本地故障。 |
| TST-010 | 建立安全与恶意包测试集 | P0 | M3 | 安全/QA | 8 | SEC-001,SEC-002,CTL-001 | 覆盖路径穿越、Zip Slip、无令牌 API、Origin 欺骗、超大文件和任意代码载荷。 |
| TST-011 | 实现需求—任务—用例追踪与测试报告 | P0 | M2 | QA | 6 | GOV-006 | 每个 P0 需求至少一个自动化或明确的人工验收用例。 |
| TST-012 | 建立发布候选回归包 | P0 | M3 | QA | 6 | TST-006,TST-007,TST-009,TST-010 | 一键生成通过率、缺陷、风险豁免、性能趋势和签字页。 |

### E14 安全、隐私、安装与运维

**目标：** 让单机应用在标准用户权限下安全安装、诊断、升级和卸载，并最小化截图及日志泄露。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| SEC-001 | 禁止任意 Python/系统命令执行 | P0 | M0 | 安全/后端 | 5 | DOM-005 | DSL、导入包和 UI 均无 exec/eval/shell 入口；插件默认关闭。 |
| SEC-002 | 实现安全解包与路径边界 | P0 | M2 | 安全/后端 | 5 | CTL-004 | 拒绝绝对路径、..、符号链接逃逸、设备路径和超限解压。 |
| SEC-003 | 实现本地 API 限权与攻击面检查 | P0 | M2 | 安全 | 5 | CTL-001,CTL-002 | 完成端口绑定、CSRF/Origin、令牌、错误信息和速率限制检查。 |
| SEC-004 | 实现资产来源、哈希和篡改检测 | P1 | M2 | 安全/后端 | 5 | DOM-008 | 检测资产在运行前与发布包一致；篡改即拒绝。 |
| SEC-005 | 实现截图隐私遮罩 | P0 | M3 | 前端/后端 | 6 | UI-005 | 可配置固定遮罩；导出和缩略图统一应用；遮罩不可被预览缓存绕过。 |
| SEC-006 | 实现日志/截图保留与清理策略 | P0 | M3 | 后端 | 5 | TRC-010 | 默认最小留存；清理有审计且不会误删发布基线。 |
| SEC-007 | 实现路径、用户名、窗口标题等脱敏 | P0 | M3 | 后端 | 4 | ENG-005 | 诊断包与导出包默认不暴露本机用户名和绝对路径。 |
| REL-001 | 实现 Windows x64 可重复构建 | P0 | M3 | 发布 | 7 | ENG-006,UI-001 | 同一提交可生成可安装制品；版本与提交哈希可查看。 |
| REL-002 | 实现安装、修复与卸载流程 | P0 | M3 | 发布/QA | 7 | REL-001 | 标准用户可安装；卸载释放热键、进程和临时文件；用户项目默认保留并提示。 |
| REL-003 | 集成代码签名与签名校验钩子 | P1 | M3 | 发布/安全 | 5 | REL-001 | 签名可在 CI 的受保护阶段执行；本地开发无需持有证书。 |
| REL-004 | 实现崩溃转储和诊断包 | P1 | M3 | 后端/发布 | 6 | ENG-005,CTL-010 | 用户可生成脱敏诊断包；包含版本、日志、健康快照但默认不含原图。 |
| REL-005 | 实现备份、恢复和首次启动检查 | P0 | M3 | 后端/前端 | 5 | VER-009 | 首次启动校验目录权限、依赖、显示环境和旧版本迁移状态。 |
| DOC-001 | 编写脚本/检测器作者指南 | P1 | M3 | 文档/开发 | 5 | DOM-007,VIS-012,FSM-010 | 包含 Schema、测试、性能和禁止能力；示例仅使用 ArenaLab。 |
| DOC-002 | 编写运行、故障处理与安全边界手册 | P0 | M3 | 文档/QA | 5 | INP-010,REL-004 | 说明急停、失焦、权限错误、黑帧、恢复、数据导出和风险边界。 |

### E15 可视化节点编辑与受限扩展（可选）

**目标：** 在 DSL 稳定后提供节点视图和受限扩展机制，且不形成第二套运行语义。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| EDT-001 | 实现 Rete.js 只读状态图适配 | P2 | M4 | 前端 | 6 | UI-011 | 图完全由 AST 生成，不保存独立业务语义。 |
| EDT-002 | 实现节点编辑到 AST 的双向映射 | P2 | M4 | 前端/后端 | 10 | EDT-001,DOM-006 | 文本、表单、节点编辑结果一致，冲突可检测。 |
| EDT-003 | 实现撤销/重做、复制粘贴与布局 | P2 | M4 | 前端 | 8 | EDT-002 | 所有操作可逆并保持稳定 ID。 |
| EDT-004 | 完成大图性能与可用性优化 | P2 | M4 | 前端/QA | 6 | EDT-002 | 基准规模下缩放、选择和编辑不卡死，性能指标达标。 |
| PLG-001 | 定义扩展能力契约与签名格式 | P2 | M4 | TL/安全 | 6 | DOM-005 | 扩展必须声明能力、版本、哈希和资源限额。 |
| PLG-002 | 实现独立进程扩展 Runner 原型 | P2 | M4 | 后端/安全 | 10 | PLG-001 | 扩展无管理员权限，无法直接访问 Input Broker，崩溃不影响按键释放。 |
| PLG-003 | 实现扩展权限审查与撤销 UI | P2 | M4 | 前端/安全 | 6 | PLG-001,PLG-002 | 安装前展示权限，运行中可撤销；撤销后立即停止调用。 |

### E16 受控目标适配与发布边界

**目标：** 只为本地、离线、自有或明确允许自动化的目标启用真实输入；受保护在线目标保持观察与 Shadow。

| 任务 ID | 任务 | 优先级 | 里程碑 | 主责 | 人日 | 依赖 | 交付/验收摘要 |
|---|---|---:|---:|---|---:|---|---|
| ADP-001 | 实现通用普通桌面应用目标模板 | P1 | M5 | 后端/QA | 6 | TGT-002,POL-002 | 包含窗口、标定、策略和测试示例，可在自有测试程序上通过 RealInput E2E。 |
| ADP-002 | 实现受保护在线目标 Shadow 模板 | P1 | M5 | 后端/安全 | 5 | POL-003 | 模板硬编码 real_input=false、unattended_schedule=false、auto_reconnect=false。 |
| ADP-003 | 建立目标适配准入评审清单 | P1 | M5 | 产品/TL/安全 | 4 | GOV-001 | 要求授权依据、风险等级、可测试环境、回滚和数据处理说明。 |
| ADP-004 | 建立目标包独立兼容与回归矩阵 | P1 | M5 | QA/CV | 6 | VER-004,TST-011 | 目标 UI 更新只重跑受影响检测器、状态机和轨迹。 |
| ADP-005 | 完成适配包发布、禁用与撤回机制 | P1 | M5 | 发布/后端 | 5 | VER-005,ADP-003 | 可远离主程序版本撤回目标包；撤回后不能启动新会话。 |


---

## 5. 推荐实施顺序与并行关系

### 5.1 关键路径

```text
GOV/ENG
  → DOM 基础 Schema
    → ArenaLab + Target/Capture
      → Policy/Input Broker
        → Vision + FSM
          → Trace/Replay
            → UI 测试中心 + Version/Release
              → 安全/长稳/UAT
```

### 5.2 可并行工作流

- **流 A：运行时安全**——Target、Policy、Input Broker、急停、看门狗。
- **流 B：视觉与测试数据**——Capture、Detector、黄金数据集、ArenaLab 故障注入。
- **流 C：DSL 与回放**——Schema、静态分析、FSM、Trace、Replay、Diff。
- **流 D：工作台 UI**——项目导航、预览、ROI、编辑器、时间轴、测试中心。
- **流 E：工程与发布**——CI、安装器、签名、诊断、备份、隐私和安全测试。

### 5.3 M0-M3 推荐迭代切片

| 迭代 | 端到端可演示结果 |
|---|---|
| I0 | 启动 ArenaLab，加载示例项目，Schema 校验通过，生成空 RunTrace |
| I1 | 选择 ArenaLab 窗口，采集并预览，框选 ROI，Shadow 模式生成 InputIntent |
| I2 | RealInput 在 ArenaLab 中执行单次动作；错误窗口、失焦和急停均安全停止 |
| I3 | 模板/颜色检测驱动状态迁移；固定感知回放结果一致 |
| I4 | 原始帧回放、差异报告和视觉指标可用；异常场景自动化回归 |
| I5 | 资产库、状态机编辑、发布包、整体回滚和安装升级打通 |
| I6 | 完成全量回归、8 小时 Shadow 长稳、UAT 和候选版签字 |

---

# 第二部分：测试与验收方案

## 6. 测试目标与策略

测试的首要目标不是证明“能持续自动操作”，而是证明：

1. **错误条件下不会输入**；
2. **任何输入都可追溯、可取消、可释放**；
3. **视觉与状态机变更可通过数据和回放发现回归**；
4. **同一版本在同一输入轨迹上结果确定**；
5. **安装、升级、崩溃和长时间运行不会留下卡键、僵尸进程或不可恢复项目**。

### 6.1 测试层级

| 层级 | 目标 | 主要工具/夹具 | 触发频率 |
|---|---|---|---|
| 静态检查 | Schema、类型、Lint、依赖、禁止 API | Schema 校验、类型检查、代码扫描 | 每次 PR |
| 单元测试 | 纯函数、策略、状态机、坐标、预算、序列化 | pytest/Vitest、FakeClock | 每次 PR |
| 属性/模糊测试 | DSL、Schema、压缩包和边界值健壮性 | 随机生成器、Fuzz 夹具 | 每日/主分支 |
| 组件测试 | Capture、Detector、Broker、Trace、Storage | FakeWindow、FakeInput、黄金帧 | 每次 PR/主分支 |
| 契约测试 | UI—Control—Runtime—Broker 消息兼容 | 固定消息样例、版本矩阵 | 每次 PR |
| 回放测试 | 感知和状态机变更差异 | 固定感知轨迹、原始帧轨迹 | 每次相关变更 |
| Windows E2E | 真窗口、真采集、真前台输入安全 | ArenaLab、专用 CI Agent | 主分支/候选版 |
| 性能测试 | 延迟、FPS、资源、轨迹开销 | 基准机、固定项目 | 每日/候选版 |
| 长稳与故障 | 内存增长、卡键、崩溃恢复、磁盘异常 | ArenaLab 故障注入 | 候选版 |
| 安全测试 | 本地 API、导入包、路径、任意代码、隐私 | 恶意包集合、端口测试 | 主分支/候选版 |
| UAT | 用户端到端工作流和可理解性 | 标准验收脚本 | 每个里程碑 |

### 6.2 测试环境矩阵

发布前至少覆盖以下组合；具体操作系统构建号在 M0 冻结并记录到候选版报告。

| 维度 | 覆盖 |
|---|---|
| 操作系统 | Windows 10 x64、Windows 11 x64；标准用户；工作台与目标同权限/目标更高权限 |
| 分辨率 | 1920×1080、2560×1440、3840×2160 |
| DPI | 100%、125%、150% |
| 显示器 | 单屏、双屏、目标跨屏、主屏切换、显示器断开 |
| 窗口状态 | 窗口化、无边框窗口、最小化、遮挡、尺寸变化、目标重启 |
| 输入 | 常见键盘布局、输入法开/关、鼠标缩放、长按和组合键 |
| 运行模式 | Observe、Shadow、Dry Run、RealInput（仅 ArenaLab/允许目标） |
| 采集适配器 | 主适配器、MSS 回退、主动切换、设备丢失 |
| 资源压力 | CPU 忙、GPU 忙、低磁盘、写入失败、帧率下降、UI 卡顿 |
| 会话状态 | 失焦、锁屏、用户切换、远程桌面连接/断开、父进程异常退出 |

### 6.3 基准机与量化口径

M0 建立至少一台项目基准机，并在报告中固定 CPU、内存、GPU、显示器、DPI 和系统构建。下列数值是首版建议门槛，允许在 M0 性能摸底后调整一次，之后变更必须走 ADR 和风险评审。

---

## 7. 发布质量闸门

### 7.1 一票否决项

出现任意一项，候选版不得发布：

- 错误窗口、Shadow/Dry Run、失焦、锁屏、会话切换或过期意图仍产生真实输入；
- 急停或异常退出后存在未释放按键；
- 任意脚本/导入包可执行 Python、Shell、DLL、系统命令或绕过 Input Broker；
- 受保护在线目标能够通过修改项目文件启用 RealInput；
- P0/P1 缺陷未关闭且无正式豁免；
- 项目迁移或回滚可导致资产、状态机和策略版本不一致；
- 轨迹不能指出实际使用的脚本、资产、策略和运行时版本；
- 导入包存在路径穿越、Zip Slip 或覆盖项目根目录之外文件的能力。

### 7.2 量化门槛

| 类别 | 首版验收门槛 |
|---|---|
| 安全用例 | P0 安全用例 100% 通过；错误目标、Shadow/Dry Run 的真实输入次数必须为 0 |
| 急停 | 基准机上从热键事件到 Broker 禁止新输入并开始释放的 p95 ≤ 150 ms；全量释放 p95 ≤ 300 ms |
| 失焦 | 失焦被检测后，不再接受新批次；已按键在 p95 ≤ 300 ms 内释放 |
| 确定性 | 同版本、同轨迹、同种子连续运行 100 次，状态/意图哈希一致率 100% |
| 状态机 | 发布脚本状态和迁移覆盖率 100%；守卫分支覆盖率 ≥ 90%；所有非终态有退出或超时 |
| 后端覆盖率 | Policy/Input/FSM 分支覆盖率 ≥ 90%；后端总体语句覆盖率 ≥ 80% |
| 前端覆盖率 | 关键 Store、协议适配和编辑器转换逻辑语句覆盖率 ≥ 75% |
| 视觉关键检测器 | 在冻结验证集上 precision、recall 均 ≥ 0.98；非关键检测器均 ≥ 0.95；未达标需降级或明确风险豁免 |
| 比例检测 | 血条/资源条类检测 MAE ≤ 0.03，P95 绝对误差 ≤ 0.05 |
| OCR | 在受控 ROI 验证集上关键字符串完全匹配率 ≥ 95%；OCR 不得作为唯一安全停止条件 |
| 采集 | 1080p 基准场景持续 ≥ 30 FPS；采集 p95 延迟 ≤ 50 ms；丢帧可观测且不积压 |
| 端到端感知 | 5 个基础检测器场景下，从帧时间戳到 PerceptionSnapshot 完成的 p95 ≤ 120 ms |
| UI 实时性 | 状态/策略事件到 UI 展示 p95 ≤ 300 ms；预览卡顿不影响 Broker 和急停 |
| 稳定性 | 8 小时 Shadow 长稳无崩溃、无卡键、无僵尸进程；内存相对稳定段增长 ≤ 15% |
| RealInput 长稳 | ArenaLab 连续 2 小时，停止后所有键释放，输入顺序错误为 0 |
| 安装升级 | 干净安装、覆盖升级、迁移失败回滚、卸载均通过；用户项目不得被静默删除 |
| 缺陷 | 0 个 P0、0 个 P1；P2 必须有明确规避、负责人和计划 |

---

## 8. 测试数据与夹具

### 8.1 黄金视觉数据集

每个 Detector 至少包含：

- 正样本、近似负样本、完全负样本；
- 三档分辨率、三档 DPI、不同窗口尺寸；
- 亮度、色偏、压缩、运动模糊、粒子/字幕遮挡；
- 模板过期、错误语言、阈值边界；
- 资产来源、标签人、版本、哈希和预期输出。

数据集必须分为训练/调参集、验证集和冻结验收集。冻结验收集不得被日常阈值调参直接查看或覆盖。

### 8.2 轨迹数据集

至少建立以下标准轨迹：

- `happy_path`：识别→状态迁移→单次输入→结果确认→终止；
- `focus_lost`：按键按下期间失焦；
- `wrong_target`：同名窗口或 HWND/PID 变化；
- `capture_black_frame`：主采集器黑帧并回退；
- `loading_timeout`：加载时间超过上限；
- `detector_flap`：置信度在阈值附近抖动；
- `budget_exhausted`：动作预算耗尽；
- `broker_crash`：输入代理异常退出；
- `disk_full`：轨迹写入失败；
- `project_upgrade_rollback`：迁移失败后恢复旧版本。

### 8.3 恶意与异常输入集

- 含 `../`、绝对路径、设备路径、超长路径、符号链接的项目包；
- 高压缩比、超大文件、重复文件、损坏 Zip；
- 未知字段、深层嵌套、循环引用、NaN/Infinity、超长正则；
- 包含 Python、PowerShell、批处理、DLL、快捷方式或可执行文件的导入包；
- 伪造签名、篡改哈希、重复版本号和降级包。

---

## 9. 核心测试用例目录

### 9.1 安全与输入代理

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| SAFE-001 | 当前前台窗口不是目标 | 批次被拒绝，真实输入 0，记录 `foreground_mismatch` |
| SAFE-002 | HWND 相同但 PID/目标实例已变化 | 批次被拒绝，要求重新人工确认 |
| SAFE-003 | 意图已过 TTL | 丢弃意图，不补发，不影响后续新会话 |
| SAFE-004 | Shadow 模式产生动作意图 | Trace 记录意图，OS 输入 0 |
| SAFE-005 | Dry Run 单步调试 | 可查看状态和意图，OS 输入 0 |
| SAFE-006 | RealInput 未经过人工闸门 | 启动被拒绝 |
| SAFE-007 | 按键按下期间目标失焦 | 取消队列并释放所有已按下键 |
| SAFE-008 | 全局急停，UI 正常 | 达到急停延迟门槛，状态进入 stopped |
| SAFE-009 | 全局急停，UI 主线程卡死 | 急停仍生效，真实输入停止 |
| SAFE-010 | Runtime 父进程崩溃 | Watchdog 释放全部按键，Broker 拒绝后续旧会话意图 |
| SAFE-011 | Broker 自身异常重启 | 不重放旧队列；键状态进入安全复位 |
| SAFE-012 | 动作数预算耗尽 | 当前及后续动作被拒绝，会话按策略停止 |
| SAFE-013 | 连续按键时间超限 | 自动释放对应键并停止 |
| SAFE-014 | 运行时长达到硬上限 | 进入不可自动恢复停止态 |
| SAFE-015 | 不在允许运行时间窗 | 会话不能进入 RealInput |
| SAFE-016 | 锁屏/用户切换/RDP 状态变化 | 立即停止，不自动抢回焦点 |
| SAFE-017 | 目标权限高于工作台 | 明确提示 UIPI/权限问题，不报告假成功 |
| SAFE-018 | Stop 被重复调用 | 幂等；无异常；所有键保持释放 |
| SAFE-019 | 两个 RealInput 会话并发启动 | 第二个被拒绝 |
| SAFE-020 | protected_online 项目手改 real_input=true | Schema/Policy 双重拒绝 |
| SAFE-021 | 掉线样式弹窗出现 | 默认停止并通知，不自动重连继续 |
| SAFE-022 | 受保护目标尝试定时自动启动 | 策略拒绝并记录原因 |

### 9.2 采集、目标与标定

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| CAP-001 | 正常选择 ArenaLab 窗口 | 客户区画面、尺寸和时间戳正确 |
| CAP-002 | 窗口移动和改变尺寸 | 几何更新；旧标定不兼容时停止 |
| CAP-003 | DPI 从 100% 切到 125% | 坐标转换正确或要求重新标定 |
| CAP-004 | 目标跨两个显示器 | 采集区域与输入坐标一致 |
| CAP-005 | 主显示器切换 | 会话停止并提示重新确认 |
| CAP-006 | 主采集器设备丢失 | 停止输入，记录错误，可安全切到 MSS |
| CAP-007 | 采集连续黑帧 | 不继续决策；进入安全停止或降级流程 |
| CAP-008 | 检测器速度低于采集速度 | 背压生效，不无限积压内存 |
| CAP-009 | ROI 超出客户区 | 校验失败或安全裁剪，不崩溃 |
| CAP-010 | 帧时间戳乱序/系统时钟跳变 | 使用单调时钟，轨迹顺序保持有效 |
| CAP-011 | 目标最小化 | 按策略停止，不用旧帧继续输入 |
| CAP-012 | 同标题的两个窗口 | 不自动猜测；要求用户确认具体实例 |

### 9.3 视觉检测

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| VIS-001 | 模板正样本 | 命中、框位置和置信度达标 |
| VIS-002 | 相似但非目标图标 | 不误报或误报率在冻结指标内 |
| VIS-003 | 阈值边界上下浮动 | 迟滞和稳定帧抑制单帧抖动 |
| VIS-004 | 使用透明/显式掩码 | 遮罩区域不参与匹配 |
| VIS-005 | 有限尺度变化 | 在配置尺度内命中，超出范围明确失败 |
| VIS-006 | 血条 0%、20%、50%、100% | 比例误差达到门槛 |
| VIS-007 | 亮度和色偏变化 | 关键检测器指标不低于门槛 |
| VIS-008 | 粒子、字幕或弹窗遮挡 | 置信度下降可观测，不产生错误稳定命中 |
| VIS-009 | 画面持续变化 | 不被判定为卡死 |
| VIS-010 | 画面静止超过阈值 | 卡死/稳定检测按配置触发 |
| VIS-011 | OCR 触发条件不成立 | OCR 不运行，不消耗额外预算 |
| VIS-012 | OCR 超时或空结果 | 返回可处理错误，不阻塞主循环 |
| VIS-013 | 模板文件被篡改 | 哈希校验失败，项目不能进入 RealInput |
| VIS-014 | 修改阈值后批量回归 | 输出样本级差异和总体指标变化 |
| VIS-015 | 检测器超出计算预算 | 低优先级检测器降频，安全检测器保留 |

### 9.4 状态机与策略

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| FSM-001 | Happy path 感知序列 | 在预期帧迁移并生成预期意图 |
| FSM-002 | 守卫条件为 false | 不迁移、不生成动作 |
| FSM-003 | stable_frames 未满足 | 保持原状态 |
| FSM-004 | 状态超时 | 进入配置的错误/停止状态 |
| FSM-005 | bounded retry 达上限 | 停止重试并走异常路由 |
| FSM-006 | 两个迁移同时满足 | 按明确优先级决定或编译报冲突 |
| FSM-007 | 引用未定义感知字段 | 静态检查失败 |
| FSM-008 | 非终态没有退出或超时 | 静态检查失败 |
| FSM-009 | 不可达状态 | 静态检查告警/失败，按发布规则处理 |
| FSM-010 | Pause 后继续 | 不补发暂停期间过期意图；重新验证目标 |
| FSM-011 | Cancel | 立即停止产生新意图并等待 Broker 清理完成 |
| FSM-012 | 同轨迹运行 100 次 | 决策哈希完全一致 |
| FSM-013 | 策略拒绝动作 | 状态机收到结构化拒绝结果，不绕过重试 |
| FSM-014 | 动作后断言未满足 | 进入指定异常出口并保留证据帧 |
| FSM-015 | 时间推进测试 | 使用 FakeClock，无真实 sleep，结果与实时一致 |

### 9.5 轨迹、回放、版本与回滚

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| TRC-001 | 一次完整运行 | 帧、感知、状态、意图、策略和执行链可关联 |
| TRC-002 | 写轨迹时进程崩溃 | 已确认事件可读取，损坏尾部可检测 |
| TRC-003 | 固定感知回放 | 状态和意图与原运行一致 |
| TRC-004 | 原始帧回放 | 感知、状态和意图均可比较 |
| TRC-005 | 修改模板 | 报告首个感知分歧与受影响迁移/测试 |
| TRC-006 | 修改状态机守卫 | 报告状态/意图分歧但视觉结果不变 |
| TRC-007 | 导出轨迹 | 默认脱敏且不包含未授权原始截图 |
| TRC-008 | 导入损坏轨迹 | 安全失败，不污染现有项目 |
| VER-001 | 发布版本 | Manifest、哈希、测试摘要和运行时范围冻结 |
| VER-002 | 发布后修改资产文件 | 运行前检测篡改并拒绝 |
| VER-003 | 整体回滚 | 状态机、资产、阈值、标定、策略一起恢复 |
| VER-004 | 升级迁移失败 | 恢复旧版本并保留失败诊断 |
| VER-005 | 导入未知签名包 | 可查看但默认不能启用 RealInput |
| VER-006 | 影响分析 | 修改资产后只列出真实依赖和必跑测试 |

### 9.6 UI、安装与安全

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| UI-001 | 新建项目并加载示例 | 对象树和默认策略正确 |
| UI-002 | 从当前帧框选 ROI | 归一化坐标和预览一致 |
| UI-003 | 表单与 YAML 同步 | 语义一致；冲突不静默覆盖 |
| UI-004 | 输入非法字段 | 精确定位错误并阻止运行 |
| UI-005 | 切换 Shadow/RealInput | 风险提示、人工闸门和状态颜色明显 |
| UI-006 | 时间轴点击事件 | 恢复对应帧、检测框、状态和策略结果 |
| UI-007 | 删除被引用资产 | 阻止删除并展示引用关系 |
| UI-008 | 测试失败 | 可跳到失败样本和首个分歧点 |
| UI-009 | UI 卡死模拟 | Broker、急停和看门狗不受影响 |
| UI-010 | 键盘操作核心流程 | 无鼠标可完成项目导航、停止和查看错误 |
| SEC-001 | 无令牌访问本地 API | 401/拒绝，不泄露敏感详情 |
| SEC-002 | 恶意 Origin/Host | 请求被拒绝 |
| SEC-003 | Zip Slip/路径穿越包 | 导入失败，项目目录之外无写入 |
| SEC-004 | 含可执行文件/脚本的项目包 | 默认拒绝或隔离，不执行 |
| SEC-005 | 超大/高压缩比包 | 达到限制后安全终止，不耗尽磁盘/内存 |
| SEC-006 | 隐私遮罩导出 | 被遮区域在缩略图、原图和诊断包中均不可见 |
| REL-001 | 干净安装 | 标准用户可启动，依赖完整 |
| REL-002 | 覆盖升级 | 项目和设置按规则迁移，失败可回滚 |
| REL-003 | 卸载 | 进程、热键、服务和临时文件被清理；项目按提示保留 |
| REL-004 | 生成诊断包 | 内容脱敏、可校验、无未授权原图 |

### 9.7 性能与长稳

| 用例 ID | 场景 | 预期结果 |
|---|---|---|
| PERF-001 | 1080p 持续采集 | FPS、延迟和丢帧达到质量门槛 |
| PERF-002 | 5 个基础检测器并行 | 端到端感知 p95 达标 |
| PERF-003 | UI 打开预览和时间轴 | UI 延迟达标，Runtime 无明显退化 |
| PERF-004 | 轨迹记录开启/关闭对比 | 轨迹开销在约定预算内并有趋势报告 |
| PERF-005 | 8 小时 Shadow | 无崩溃、卡键、僵尸进程，内存增长达标 |
| PERF-006 | 2 小时 ArenaLab RealInput | 输入顺序错误 0，停止后键状态为空 |
| PERF-007 | 磁盘接近满 | 停止保存非关键帧并安全停止，不破坏项目 |
| PERF-008 | 高 CPU/帧率下降 | 低优先级检测降频，安全条件仍能处理 |
| PERF-009 | 大项目打开 | 在冻结基准规模下启动与搜索时间达标 |
| PERF-010 | 10 万事件时间轴 | 分页/虚拟化有效，UI 不冻结 |

---

## 10. 十五个 P0 验收用例的执行步骤

### AC-P0-01 错误目标窗口零输入

**前置条件：** ArenaLab 与记事本同时打开；项目绑定 ArenaLab；Input Broker 使用可观测的 OS 输入监视器。

**步骤：**
1. 启动 RealInput 会话并通过人工闸门；
2. 在意图生成前切换前台到记事本；
3. 让状态机产生 10 个键盘/鼠标意图；
4. 检查记事本、ArenaLab、Broker 日志和 RunTrace。

**通过标准：** 两个应用实际输入均为 0；10 个意图全部标记 `foreground_mismatch`；无按键处于 pressed 状态。

### AC-P0-02 Shadow/Dry Run 零真实输入

**步骤：** 分别以 Shadow 和 Dry Run 执行完整 happy_path 轨迹。

**通过标准：** 状态机和意图完整产生；FakeInputSink/Trace 有记录；Win32 适配器调用次数为 0。

### AC-P0-03 按键按下期间失焦

**步骤：** 执行一个 3 秒长按；500 ms 后切换前台窗口。

**通过标准：** 在门槛内取消后续输入并释放按键；ArenaLab Oracle 显示 key-up；会话不能自动恢复。

### AC-P0-04 UI 卡死时全局急停

**步骤：** 故意阻塞 UI 主线程；运行连续动作；触发全局急停。

**通过标准：** 急停和释放达到延迟门槛；UI 是否恢复不影响 Broker；Trace 记录触发源。

### AC-P0-05 父进程崩溃清理

**步骤：** 输入代理处于长按状态时强制终止 Runtime/Control Plane。

**通过标准：** Watchdog 在门槛内释放键；Broker 不接受旧 session 的后续请求；重启后旧会话标记 interrupted。

### AC-P0-06 protected_online 硬锁

**步骤：** 导入受保护在线目标模板；手工修改 YAML 试图启用 `real_input`、定时启动和自动重连。

**通过标准：** Schema 或 Policy 在运行前拒绝；UI 不提供绕过按钮；日志包含明确规则 ID。

### AC-P0-07 动作预算与时间上限

**步骤：** 设置每分钟 20 个动作、会话上限 2 分钟；运行高频场景。

**通过标准：** 第 21 个动作被拒绝；到达 2 分钟后会话停止且不可由状态机恢复。

### AC-P0-08 同轨迹确定性

**步骤：** 使用固定感知轨迹和种子连续执行 100 次。

**通过标准：** 每次状态序列、意图序列、拒绝序列和最终哈希完全一致。

### AC-P0-09 原始帧回放发现视觉回归

**步骤：** 基于已通过的轨迹提高模板阈值，使部分样本漏检；运行回放。

**通过标准：** 报告指出首个视觉分歧、样本、检测器、受影响迁移和用例；发布闸门失败。

### AC-P0-10 非终态无退出静态阻断

**步骤：** 创建无迁移、无超时、无终止声明的状态。

**通过标准：** 编译失败并定位状态；项目不能进入运行或发布。

### AC-P0-11 恶意项目包隔离

**步骤：** 导入包含 `../`、绝对路径、高压缩比和可执行文件的项目包。

**通过标准：** 导入安全失败；项目根目录外无文件变化；应用保持可用；安全事件被记录。

### AC-P0-12 版本整体回滚

**步骤：** 发布 v1；同时修改状态机、模板、阈值、标定和策略并发布 v2；回滚到 v1。

**通过标准：** 五类对象全部恢复 v1 哈希；v2 历史仍可审计；新运行明确使用 v1。

### AC-P0-13 采集黑帧安全降级

**步骤：** 在 RealInput 前注入连续黑帧/设备丢失。

**通过标准：** 不使用旧帧继续决策；先停止输入，再尝试回退；回退成功也需要重新确认目标/标定。

### AC-P0-14 迁移失败恢复

**步骤：** 构造会在中途失败的 Schema 迁移；执行升级。

**通过标准：** 原项目可再次打开；备份存在；无半迁移对象；诊断包含失败步骤。

### AC-P0-15 8 小时 Shadow 长稳

**步骤：** 在基准机上运行包含视觉、状态机、轨迹、预览和故障注入的 8 小时场景。

**通过标准：** 无崩溃、真实输入 0、无僵尸进程、无卡键、内存增长达标、所有注入故障有正确事件和恢复/停止结果。

---

## 11. 需求追踪矩阵

| 需求 ID | 需求 | 主要开发任务 | 主要测试 |
|---|---|---|---|
| REQ-001 | 视觉感知→条件决策→前台执行闭环 | CAP-*, VIS-*, FSM-*, INP-* | CAP-001~012、VIS-001~015、FSM-001~015 |
| REQ-002 | 仅前台、单目标、错误目标零输入 | TGT-002/003/009、INP-005/008、CTL-006 | SAFE-001/002/007/016/019 |
| REQ-003 | 不注入、不读内存、不碰封包、不绕过反作弊 | GOV-001、SEC-001、POL-003 | SAFE-020/022、SEC-004 |
| REQ-004 | 模板、颜色、变化和 ROI OCR | VIS-002~009 | VIS-001~015 |
| REQ-005 | 状态机、超时、有界重试和异常出口 | FSM-001~010、DOM-007 | FSM-001~015 |
| REQ-006 | 全局急停、失焦停止、按键释放 | INP-004/006/007/008 | SAFE-007~011、AC-P0-03~05 |
| REQ-007 | 运行时长、时间窗和动作配额 | POL-004/005 | SAFE-012~015、AC-P0-07 |
| REQ-008 | 脚本仓库、版本、导入导出与回滚 | CTL-003/004、VER-* | VER-001~006、AC-P0-12/14 |
| REQ-009 | 实时识别预览和运行时间轴 | UI-004/007/013/014、TRC-008 | UI-002/006/009、PERF-003/010 |
| REQ-010 | Dry Run/Shadow Mode | POL-002、INP-002 | SAFE-004/005、AC-P0-02/15 |
| REQ-011 | 测试、回放和影响分析 | TRC-005~007、VER-004、TST-* | TRC-003~006、AC-P0-08/09 |
| REQ-012 | 隐私遮罩和留存 | SEC-005/006/007、TRC-009/010 | SEC-006、TRC-007 |
| REQ-013 | Windows 安装、升级、诊断与恢复 | REL-001~005、VER-009 | REL-001~004、AC-P0-14 |
| REQ-014 | ArenaLab 可控测试场 | LAB-* | TST-007、全部 AC-P0 ArenaLab 用例 |

---

## 12. 里程碑验收清单

### 12.1 M0 验收

- [ ] 架构不变量、禁止能力和威胁模型完成评审；
- [ ] Monorepo、Windows CI、日志和测试报告归档可用；
- [ ] 核心 Schema、示例项目、能力清单和 RunTrace 格式通过正反样例；
- [ ] ArenaLab 可用固定种子生成确定帧和 Oracle；
- [ ] 无模块直接调用输入库，InputIntent 契约已冻结；
- [ ] protected_online 默认策略明确为无真实输入。

**M0 证据：** ADR、Schema 包、CI 链接、ArenaLab 构建、示例项目、威胁模型、评审纪要。

### 12.2 M1 验收

- [ ] 桌面壳、目标选择、采集预览、会话管理可用；
- [ ] Observe/Shadow/DryRun/RealInput 模式清晰；
- [ ] Input Broker、前台复核、TTL、动作预算、急停、失焦和 Watchdog 可用；
- [ ] SAFE-001~022 中 M1 适用用例 100% 通过；
- [ ] AC-P0-01~07 通过；
- [ ] 错目标、Shadow、Dry Run 的真实输入次数为 0；
- [ ] 停止、崩溃和失焦后无卡键。

**M1 证据：** Windows E2E 报告、Broker 审计、急停延迟、按键状态记录、演示录像/轨迹。

### 12.3 M2 验收

- [ ] 模板、颜色条、像素、变化/稳定和 ROI OCR 可用；
- [ ] 视觉黄金数据集和指标达到门槛；
- [ ] DSL 编译、静态检查、状态机、超时、有界重试和确定性运行可用；
- [ ] 固定感知与原始帧回放可用；
- [ ] 差异报告能定位首个分歧与受影响测试；
- [ ] AC-P0-08~10、13 通过；
- [ ] ArenaLab 端到端 happy path 与异常路径通过。

**M2 证据：** 视觉指标报告、状态/迁移覆盖、回放差异、性能报告、测试追踪矩阵。

### 12.4 M3 验收

- [ ] 资产库、标定、检测器、状态机、测试中心和时间轴工作流完整；
- [ ] 发布包冻结所有依赖，支持签名、导入预检、版本比较和整体回滚；
- [ ] 隐私遮罩、留存、脱敏、诊断包可用；
- [ ] 干净安装、升级、迁移失败回滚和卸载通过；
- [ ] 安全恶意包测试通过；
- [ ] 8 小时 Shadow、2 小时 ArenaLab RealInput 长稳通过；
- [ ] AC-P0-11~15 通过；
- [ ] 全量 P0/P1 用例达到发布闸门；
- [ ] 产品、技术、QA、安全/发布完成签字。

**M3 证据：** 候选版安装包、SBOM/签名、全量测试报告、UAT 记录、缺陷清单、回滚演示、发布签字页。

---

## 13. 用户验收测试（UAT）

| UAT ID | 业务场景 | 验收结果 |
|---|---|---|
| UAT-001 | 新建 ArenaLab 项目，选择目标并完成标定 | 项目结构正确，目标身份和标定状态清楚 |
| UAT-002 | 从实时帧框选 ROI、创建模板并配置检测器 | 非开发人员不改 Python 即可完成并看到置信度 |
| UAT-003 | 配置状态机并在 Shadow 模式运行 | 能看到状态、候选迁移、意图和策略结果；真实输入 0 |
| UAT-004 | 通过人工闸门在 ArenaLab 执行 RealInput | 仅目标窗口收到预期输入，动作可追溯 |
| UAT-005 | 运行期间切走窗口并恢复 | 系统停止且不自动继续，用户需重新确认 |
| UAT-006 | 触发急停 | 动作立即停止，按键释放，原因清晰 |
| UAT-007 | 修改模板/阈值并运行影响分析 | 可看到受影响检测器、迁移和测试，能比较回放差异 |
| UAT-008 | 发布 v1、修改并发布 v2、回滚 v1 | 完整包回滚且运行记录版本准确 |
| UAT-009 | 导出脱敏轨迹给他人排障 | 不含遮罩区域和本机敏感路径，导入后可回放 |
| UAT-010 | 安装升级失败后恢复 | 旧版本与项目可正常打开，不丢数据 |
| UAT-011 | 加载 protected_online Shadow 模板 | UI 明确只允许观察/Shadow，不能启用真实输入或定时无人值守 |
| UAT-012 | 查看诊断与运行报告 | 可理解性能、丢帧、拒绝、急停、异常和建议处理方式 |

UAT 通过标准：UAT-001~012 全部通过；不存在“需要开发人员手改配置才能完成”的阻塞步骤；关键风险提示不允许被永久隐藏。

---

## 14. 缺陷分级与发布规则

| 级别 | 定义 | 示例 | 发布处理 |
|---|---|---|---|
| P0 致命 | 造成错误真实输入、卡键、越权执行、数据破坏或边界绕过 | Shadow 产生输入、失焦后继续、导入包执行代码 | 立即阻断，必须修复并新增自动回归 |
| P1 严重 | 核心闭环不可用、结果不可信、无法回滚或隐私泄露 | 回放不一致、资产版本错乱、遮罩失效 | 阻断发布，原则上不得豁免 |
| P2 一般 | 有明确规避的功能缺陷或兼容问题 | 某 DPI 预览错位但被安全停止 | 可有限豁免，需负责人、规避和修复版本 |
| P3 轻微 | 文案、布局或低影响体验问题 | 非关键提示文案不统一 | 可进入后续迭代 |

缺陷关闭必须附：根因、修复提交、影响范围、自动化回归用例和必要的轨迹/样本。

---

## 15. 候选版验收流程

```text
开发自测
  → PR 静态/单元/契约
  → 主分支回放与 Windows E2E
  → 候选版全量回归
  → 性能/长稳/故障注入
  → 安全与恶意包测试
  → 安装/升级/回滚
  → UAT
  → 缺陷与风险评审
  → 产品 + TL + QA + 发布签字
```

### 15.1 候选版必须归档的产物

- 安装包、便携诊断构建（如保留）、符号文件；
- 提交哈希、版本 Manifest、Schema 与运行时兼容范围；
- SBOM、依赖锁文件、签名与哈希；
- 单元/组件/契约/回放/E2E/安全/性能/长稳报告；
- 视觉指标、状态机覆盖、需求追踪矩阵；
- 已知问题、风险豁免、回滚步骤和恢复包；
- UAT 签字页与发布批准记录。

### 15.2 签字职责

| 角色 | 签字内容 |
|---|---|
| 产品负责人 | 范围、UAT、风险提示和非目标符合预期 |
| 技术负责人 | 架构不变量、兼容性、回滚和技术债可接受 |
| QA 负责人 | 测试覆盖、结果、缺陷和质量闸门真实完整 |
| 安全/发布负责人 | 禁止能力、导入包、本地 API、签名、安装升级和制品链可接受 |

---

## 16. 上线后回归与变更控制

### 16.1 任何下列变更都必须触发影响分析

- 模板图、掩码、阈值、ROI、标定或 OCR 配置；
- Detector 算法、OpenCV/OCR 依赖、色彩空间或缩放策略；
- 感知字段、状态机守卫、迁移优先级、超时和重试；
- Policy、动作预算、Input Broker、热键或目标匹配规则；
- Schema、项目包、轨迹格式、IPC 或运行时版本；
- Windows 构建、显示驱动、采集后端和打包方式。

### 16.2 最小回归集选择规则

```text
变更对象
  → 直接依赖
    → 感知字段/状态迁移
      → 相关轨迹与测试
        → 受影响项目包
```

P0 安全回归、Schema/导入安全和 ArenaLab 基本闭环始终执行，不得因“无直接依赖”而跳过。

### 16.3 发布后发现严重问题

1. 立即禁用有问题的项目/适配包或 RealInput 能力；
2. 保存脱敏轨迹、版本 Manifest 和诊断包；
3. 回滚完整发布包，不做局部文件替换；
4. 建立最小复现轨迹和自动化测试；
5. 修复后重新执行影响范围测试和全部 P0 安全回归；
6. 在变更记录中说明根因、影响、检测缺口和防复发措施。

---

## 17. 首版交付物清单

### 17.1 代码与制品

- Windows 工作台安装包；
- Control Plane、Runtime Engine、Input Broker、ArenaLab；
- Capture、Vision、FSM、Policy、Trace、Test Kit 包；
- 示例项目和冻结验收数据集；
- 发布包、回滚包、Schema 和迁移工具。

### 17.2 文档

- 架构与 ADR；
- 安全边界、威胁模型与禁止能力；
- 脚本、检测器、测试和项目包作者指南；
- 安装、升级、备份、恢复、急停和排障手册；
- API/IPC/Schema/Trace 格式说明；
- 测试报告、UAT、已知问题和发布签字。

### 17.3 首版成功定义

首版成功不以任何受保护在线游戏的无人值守自动化效果衡量，而以以下结果衡量：

- ArenaLab 上完整视觉闭环可由非开发人员配置、观察、测试、发布和回滚；
- 错误窗口、失焦、急停、崩溃、锁屏和 Shadow/Dry Run 条件下安全行为可被自动证明；
- 视觉、状态机和资产变化能通过黄金数据和回放在发布前暴露；
- 所有真实输入有明确因果链、策略结果和执行结果；
- 安装、升级、长稳、诊断和数据隐私达到本方案质量闸门。

---

## 附录 A：建议仓库结构

```text
visual-automation-workbench/
├─ apps/
│  ├─ workbench-ui/
│  ├─ desktop-shell/
│  └─ arena-lab/
├─ services/
│  ├─ control-plane/
│  ├─ runtime-engine/
│  └─ input-broker/
├─ packages/
│  ├─ domain-model/
│  ├─ capture-api/
│  ├─ capture-win/
│  ├─ capture-mss/
│  ├─ vision-core/
│  ├─ detector-opencv/
│  ├─ detector-ocr/
│  ├─ state-machine/
│  ├─ policy-engine/
│  ├─ trace-format/
│  └─ test-kit/
├─ schemas/
├─ examples/arena-lab-demo/
├─ tests/
│  ├─ unit/
│  ├─ contract/
│  ├─ replay/
│  ├─ security/
│  └─ e2e-windows/
├─ docs/
│  ├─ architecture/
│  ├─ adr/
│  ├─ safety/
│  ├─ authoring/
│  └─ operations/
└─ tools/
   ├─ asset-audit/
   ├─ trace-diff/
   ├─ performance-bench/
   └─ release-packager/
```

## 附录 B：建议项目包结构

```text
project/
├─ project.yaml
├─ targets/
├─ policies/
├─ calibrations/
├─ assets/
│  ├─ templates/
│  ├─ masks/
│  └─ labels/
├─ detectors/
├─ machines/
├─ tests/
│  ├─ perception/
│  ├─ machine/
│  ├─ replay/
│  └─ e2e/
├─ releases/
└─ traces/
```

## 附录 C：任务导入字段建议

将第 4 章任务导入 Jira、Linear 或 GitHub Projects 时，建议保留以下字段：

```text
Task ID / Epic / Summary / Description / Priority / Milestone /
Owner Role / Estimate PD / Dependencies / Acceptance Criteria /
Test Case IDs / Security Impact / Schema Impact / Release Note
```

---

*文档结束。*
